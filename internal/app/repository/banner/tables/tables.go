package tables

var (
	FeatureTable = `features`
	ID           = `id`
	Name         = `name`
	TagTable     = `tags`
	BannerTable  = `banners`
	Content      = `content`
	FeatureID    = `feature_id`
	TagID        = `tag_id`
	IsActive     = `is_active`
	CreatedAt    = `created_at`
	UpdatedAt    = `updated_at`
	Version      = `version`
)
